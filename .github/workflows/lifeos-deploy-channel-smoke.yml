name: LifeOS deploy channel smoke

on:
  push:
    branches: [main]
    paths:
      - .lifeos-deploy-channel-smoke
      - .github/workflows/lifeos-deploy-channel-smoke.yml

permissions:
  contents: read

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'public-actions-ready'
